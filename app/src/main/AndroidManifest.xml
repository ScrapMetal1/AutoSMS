<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />


    <!-- Optional permission for better reliability -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Vertex AI / Firebase calls. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Used by the AI auto-reply contact picker only; rules can also be created
         from a raw phone number with no contacts access. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Deliberately NOT requested: RECEIVE_SMS / READ_SMS. Those are restricted
         by Google Play's SMS & Call Log policy to default SMS handlers. We use
         NotificationListenerService + the system messenger's RemoteInput Reply
         action instead, which keeps us policy-compliant. -->

    <!-- Declare telephony feature as not required for ChromeOS compatibility -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />


    <application
        android:name=".AutoSMSApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_autosms"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_autosms_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoSMS.NoActionBar"
        android:largeHeap="false"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.App.Starting"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.AddEditScheduleActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />

        <!-- AI auto-reply screens (premium feature). -->
        <activity
            android:name=".ui.AutoReplyListActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.AddEditAutoReplyRuleActivity"
            android:exported="false"
            android:parentActivityName=".ui.AutoReplyListActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.AutoReplySetupActivity"
            android:exported="false"
            android:parentActivityName=".ui.AutoReplyListActivity"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".ui.PremiumActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Watches notifications posted by the user's SMS app and fulfils the
             notification's RemoteInput Reply action with a Gemini-generated reply. -->
        <service
            android:name=".service.SmsNotificationListenerService"
            android:exported="true"
            android:label="@string/auto_reply_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- gets woken by AlarmManager when it's time to send; kicks off the SMS job -->
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:exported="false" />

        <!-- alarms don't survive reboot, so we reschedule all enabled ones here.
             BOOT_COMPLETED and MY_PACKAGE_REPLACED must be in separate filters because
             putting <data scheme="package"> in the same filter would block BOOT_COMPLETED
             (it carries no data URI, so the data test fails and the broadcast is never delivered). -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest> 