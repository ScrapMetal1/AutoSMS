<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Material 3 DayNight theme.
         Dynamic color is applied at runtime via DynamicColors in
         AutoSMSApplication on Android 12+; on older devices these static
         tokens take effect. -->
    <style name="Theme.AutoSMS" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/md_primary</item>
        <item name="colorOnPrimary">@color/md_on_primary</item>
        <item name="colorPrimaryContainer">@color/md_primary_container</item>
        <item name="colorOnPrimaryContainer">@color/md_on_primary_container</item>
        <item name="colorSecondary">@color/md_secondary</item>
        <item name="colorOnSecondary">@color/md_on_secondary</item>
        <item name="colorSecondaryContainer">@color/md_secondary_container</item>
        <item name="colorOnSecondaryContainer">@color/md_on_secondary_container</item>
        <item name="colorTertiary">@color/md_tertiary</item>
        <item name="colorOnTertiary">@color/md_on_tertiary</item>
        <item name="colorTertiaryContainer">@color/md_tertiary_container</item>
        <item name="colorOnTertiaryContainer">@color/md_on_tertiary_container</item>
        <item name="colorError">@color/md_error</item>
        <item name="colorOnError">@color/md_on_error</item>
        <item name="android:colorBackground">@color/md_background</item>
        <item name="colorOnBackground">@color/md_on_background</item>
        <item name="colorSurface">@color/md_surface</item>
        <item name="colorOnSurface">@color/md_on_surface</item>
        <item name="colorSurfaceVariant">@color/md_surface_variant</item>
        <item name="colorOnSurfaceVariant">@color/md_on_surface_variant</item>
        <item name="colorOutline">@color/md_outline</item>

        <!-- Edge-to-edge: status + nav bars become transparent and content
             draws under them. Each activity then applies window insets via
             ViewCompat.setOnApplyWindowInsetsListener (already present on
             every screen we ship). -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
    </style>

    <style name="Theme.AutoSMS.NoActionBar" parent="Theme.AutoSMS">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- AppBar uses the on-surface palette so toolbar icons match the rest of
         the screen rather than inverting on a primary-coloured background. -->
    <style name="Theme.AutoSMS.AppBarOverlay" parent="ThemeOverlay.Material3.ActionBar" />
    <style name="Theme.AutoSMS.PopupOverlay" parent="ThemeOverlay.Material3" />

    <!-- Reusable building blocks for the AI screens. Centralised so visual
         tweaks (corner radius, spacing) propagate everywhere. -->
    <style name="AutoSMS.SectionHeader" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAppearance">?attr/textAppearanceLabelLarge</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:letterSpacing">0.08</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="AutoSMS.OutlinedCard" parent="Widget.Material3.CardView.Outlined">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">0dp</item>
        <item name="cardBackgroundColor">?attr/colorSurface</item>
        <item name="strokeColor">?attr/colorOutline</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">?attr/colorPrimaryContainer</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_autosms_round</item>
        <item name="postSplashScreenTheme">@style/Theme.AutoSMS.NoActionBar</item>
    </style>

</resources>
